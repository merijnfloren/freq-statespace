from setuptools import setup, find_packages

NAME = 'sysid-nonlinear-lfr'
VERSION = '0.1.0'
AUTHOR = 'Merijn Floren'

setup(
    name=NAME,
    version=VERSION,
    author=AUTHOR,
    packages=find_packages()
)
